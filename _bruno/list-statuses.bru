meta {
  name: List statuses
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/statuses
  body: none
  auth: none
}

docs {
  ```ts
  type Response = Array<{
    id: "OPEN" | "IN_TRANSIT" | "DELIVERED";
  }>;
  ```
}
