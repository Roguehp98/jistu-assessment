meta {
  name: Get updated shipment
  type: http
  seq: 6
}

get {
  url: {{baseUrl}}/shipments/{{createdShipmentId}}
  body: none
  auth: none
}

docs {
  ```ts
  type Response = {
    id: string;
    client_name: string;
    label: string;
    status: "OPEN" | "IN_TRANSIT" | "DELIVERED";
    arrival_date: string;
    delivery_by_date: string;
    eta: string;
    warehouse_id: string;
    lat: number;
    lng: number;
  };
  ```
}
