meta {
  name: List assignments
  type: http
  seq: 11
}

get {
  url: {{baseUrl}}/api/assignments?status=OPEN&_sort=label
  body: none
  auth: none
}

params:query {
  status: OPEN
  _sort: label
}

docs {
  ```ts
  type Assignment = {
    id: string;
    label: string;
    status: "OPEN" | "COMPLETED";
    clients: string[];
    shipment_count: number;
    updated_at: string;
  };

  type Response = {
    data: { data: Assignment[] };
    message: string;
    status: "SUCCESS";
  };
  ```
}
