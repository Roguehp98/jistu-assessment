# API — `_bruno/`

Server API = **Bruno collection** in `_bruno/`. **SSOT** for public surface. Hand-kept, not generated.

## Explore

- Open `_bruno/` in Bruno (https://www.usebruno.com/) — picks up `bruno.json`.
- Or read `.bru` directly — plain text.
